{{ form_start(form) }}
    {{ form_widget(form) }}
    {# {{ form_widget(form.etat) }} #}
    <div class="text-end">
 
    <button class="btn btn-primary">{{ button_label|default('Valider') }}</button>
</div>
{{ form_end(form) }}


