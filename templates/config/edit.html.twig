{% extends 'base_admin.html.twig' %}
{% block body %}
	<div class="page-wrapper">
		<div class="page-breadcrumb">
			<h1 class="text-center">
				Configuration de site
			</h1>
		</div>
		{{ include('config/_form.html.twig') }}
		<div class="text-center mt-2">
			<a href="{{ path('app_conseillerListe') }}">Retour à la liste</a>
		</div>

	</div>

{% endblock %}

{# {% block title %}Edit Config{% endblock %}

{% block body %}
    <h1>Edit Config</h1>

    {{ include('config/_form.html.twig', {'button_label': 'Update'}) }} 

    <a href="{{ path('app_config_index') }}">back to list</a>

    {{ include('config/_delete_form.html.twig') }} 
{% endblock %}  #}
